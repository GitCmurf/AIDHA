# Changelog

Canonical changelog lives at `docs/60-devex/changelog.md` and follows the DocOps metadata/versioning rules. Add entries there; this file exists for tooling that expects a root-level `CHANGELOG.md`.
